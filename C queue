#include <stdio.h>
#define MAX 100

typedef struct {
    int arr[MAX];
    int front, rear;
} Queue;

void init(Queue* q){ q->front=0; q->rear=-1; }

void enqueue(Queue* q, int x){
    if(q->rear==MAX-1) printf("Cola llena\n");
    else q->arr[++q->rear]=x;
}

void dequeue(Queue* q){
    if(q->front>q->rear) printf("Cola vacia\n");
    else printf("Dequeue: %d\n", q->arr[q->front++]);
}

int main(){
    Queue q; init(&q);
    enqueue(&q,10); enqueue(&q,20);
    dequeue(&q); dequeue(&q); dequeue(&q);
    return 0;
}
